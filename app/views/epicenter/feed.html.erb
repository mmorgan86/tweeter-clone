<h1> Feed of <%= current_user.username %> </h1>


<div>
  <% @following_tweets.each do |tweet| %>
    <div>
	    <p>@<%= link_to tweet.user.username,  show_user_path(id: tweet.user_id) %></p>
	    <p><%= tweet.message.html_safe %></p>
	  </div>
  <% end %>
</div>
